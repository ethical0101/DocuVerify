LLM_PROVIDER=
LLM_API_KEY=
LLM_MODEL=

# Optional: JWT signing secret for enterprise auth. If unset, one is generated
# automatically on first run and persisted to data/.jwt_secret (gitignored).
JWT_SECRET=
